import React from 'react';
import './index.css';

const Ship = () => (
  <div className="ship">
    <div className={'ship__cab'} />
    <div className={'ship__engine'}>
      <div className={'ship__flame'} />
    </div>

  </div>
);

export default Ship;
